#include "main.h"
/**
 * function - prints absolute number of int n;
 * function - _abs prints absolute value
 * n - the parametwe for int
 *
 * Return: Always (0)
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (0 - n);
	}
}
